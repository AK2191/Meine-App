rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /change_players/{id} { allow read, write: if true; }
    match /change_completions/{id} { allow read, write: if true; }
    match /change_challenges/{id} { allow read, write: if true; }
  }
}
